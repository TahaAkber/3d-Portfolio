# MediaPipe 0.10.17 references a source-map file that is not shipped in its
# npm package. Disabling generated bundle source maps prevents CRA's
# source-map-loader from trying to load that missing third-party file.
GENERATE_SOURCEMAP=false
